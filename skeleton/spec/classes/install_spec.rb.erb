require 'spec_helper'

describe '<%= metadata.name %>::install' do
  describe '<%= metadata.name %>::install class on RedHat' do
    let(:facts) {{
      :osfamily => 'RedHat',
    }}

    it { should contain_package('foo') }
  end

  describe '<%= metadata.name %>::install class on Debian' do
    let(:facts) {{
      :osfamily => 'Debian',
    }}

    it { should contain_package('foo') }
  end
end
