require 'spec_helper'

describe '<%= metadata.name %>::service' do
  describe '<%= metadata.name %>::service class on RedHat' do
    let(:facts) {{
      :osfamily => 'RedHat',
    }}

    it { should contain_service('foo') }
  end

  describe '<%= metadata.name %>::service class on Debian' do
    let(:facts) {{
      :osfamily => 'Debian',
    }}

    it { should contain_service('foo') }
  end
end

